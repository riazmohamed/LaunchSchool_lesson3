# question1

numbers = [1, 2, 2, 3]
numbers.uniq # Here the method 'uniq' does not mutate the original array

puts numbers

# this above program should print out 1, 2, 2, 3 in sequence in each line
# the program returns nil as the method puts in the last line returns nil
